__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
*.log
.env
.venv/

# Vendored .NET service configs — contain live credentials, must never be tracked.
# Files remain on disk for the legacy services; rotate any creds already in history.
*.exe.config

# Proprietary partner EDI specs + store master (do NOT commit to public repo)
docs/animates/
