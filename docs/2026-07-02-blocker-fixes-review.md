# Blocker-fix diff — adversarial review record (2026-07-02)

3-lens review + adjudication of the gate-review fix implementation. Verdict: **SHIP-AFTER-MUST-FIX**.

## Must-fix before deploy (ALL FIXED same day — see commit)

1. [SS-5 reclamp guts confirmed SOs] models/edi_processor.py:1026 — apply_change_order runs reclamp_order_lines on the SO unconditionally; _dc_available_qty uses free_qty which nets the order's OWN reservation (mml_roq_forecast product_fulfilment_ext.py:15-27), and Briscoes auto_confirm_clean + SS-3 force-assign means the SO is confirmed+reserved by ORDCHG-approve time. Any approved ORDCHG (even date-only — the reclamp sweeps every line) clamps fully-reserved lines toward 0, cuts the delivery moves, and the ORDRSP tells Briscoes 'cannot supply' stock reserved for that very PO. FIX: guard the reclamp with `if so.state == 'draft':` (mirroring edi_order_review.py:328), or add each line's own reserved outgoing-move qty back into `available` before _reclamp_line. Note: dormant while the live partner row is oos_policy='backorder', but the short_ship flip is a config-only go-live step — this must be fixed in code before deploy so the flip cannot arm it. Tests never cover it (test_ordchg_availability_gate.py sets auto_confirm_clean=False).

2. [Reclamp clobbers operator corrections upward] models/edi_order_review.py:259 (action_approve_corrected → _reclamp_before_confirm) with models/edi_processor.py:41-51 — _reclamp_line returns min(edi_ordered_qty, available) and writes UP as well as down, so an operator who deliberately reduces a draft line (e.g. 10→4 with 10 in stock) and clicks 'Approve with Corrections' has the qty silently restored to 10, confirmed, reserved, and ACKed — the exact qty the human rejected ships anyway. Violates the method's own contract ('confirm SO at current, possibly manually corrected, line values'). FIX: on the corrected path clamp only downward — pass a flag so reclamp uses basis = min(sol.product_uom_qty, edi_ordered_qty), i.e. new_qty = min(current, available); keep the ordered-basis up-restore only for plain action_approve of untouched parse-clamped lines. Same short_ship dormancy caveat as above.

3. [IDEM-3 residual double-ORDRSP] models/edi_order_review.py:556-561 — the ack_sent/success row written AFTER handler.upload_file is never committed at that point (the pre-upload claim at :524-538 IS committed). A rollback/worker death after upload — concretely: multi-select action_approve where a later record raises the state-check UserError at :231, rolling back the request while earlier approvals were already durably committed by their claim commits — leaves the exchange claim-only. The recovery path (:541) re-uploads whenever the filename is absent from the outbox, but an EDIS/VAN sweep produces exactly that absence, so the retry delivers a second ORDRSP for the same PO — the double-respond IDEM-3 exists to prevent. Live regardless of oos_policy. FIX: immediately after the ack_sent/success Log.log at :561, add the same guarded commit + poll-lock re-acquire pattern as the claim commit (`if not _commit_suppressed(self.env): self.env.cr.commit(); <re-acquire if edi_reservation_warned set in context>`), shrinking the unprotected window to near zero.

## Backlog — should fix soon (accepted risk for now)

1. [Premature + permanently-suppressed ACK on partially-failed multi-store file] models/edi_processor.py:183-247 (retry_pending_acks) — a failed store's savepoint leaves NO review row, so the pending_review gate (:217-222 and _queue_ack :472) is blind to it; if the store failure persists past one 30-min retry-cron tick, the ACK goes out early and BriscoesIDOCParser has_review_data marks the failed store's POSEX lines REJECTED (0 + ABGRU 11). When the store later processes, _queue_ack computes the SAME exchange filename and skips on the existing success row — the corrected ORDRSP is never sent. Fix: retry_pending_acks should skip exchanges whose inbound file has no file_download/success dedup marker yet (file still in retry).

2. [Absent-from-outbox is not proof of non-delivery] models/edi_order_review.py:539-554 — even with the must-fix commit, a true FTP ghost-success (data stored, 226 lost, ack_sent/error logged) followed by a VAN sweep before the retry cron still re-uploads a delivered ORDRSP. The mechanism cannot distinguish 'never landed' from 'landed and swept'. Make the absent-after-claim branch alert / route to manual confirmation instead of silently re-uploading (or check the VAN archive folder if EDIS exposes one).

3. [env.company-scoped SO dedup] models/edi_processor.py:1229-1233 (_find_existing_so) + :600 (SO create) both use ambient self.env.company; the unique index (models/sale_order.py:27-33) keys on company_id so it will NOT catch a cross-company duplicate. A partially-failed file retried by a user whose active company differs from the cron user's creates duplicate live SOs (double shipment) and drops ORDCHGs ('no matching SO found', :899-907). Dormant while MML is strictly single-company. Fix: scope dedup AND so_vals company to the trading partner's company (partner.partner_id.company_id or a company field on edi.trading.partner).

4. [ftplib NLST-550-on-empty-outbox wedge] models/edi_ftp.py:161-176 — some FTP servers answer 550 to NLST of an empty directory; the EDIS outbox is empty in steady state (VAN sweeps it), so every post-claim verify would raise EDIFTPError and the ORDRSP wedges permanently with only rate-limited error logs. Verify the live EDIS server's empty-NLST behavior once, or catch the benign 'no files' 550 and return [].

5. [_poll_commit commit/re-lock gap → false failure signals] models/edi_processor.py:420-421 — cr.commit() releases the advisory lock before re-acquire; a blocked concurrent poll wakes first, dup-skips the file by the committed marker and renames it; the winner's move_to_processed (:374) then raises (edi_ftp.py:207-222 raises on missing source) → per-file error log, OPS-25 alert, breaker increment, _send_file_responses skipped (ACK delayed to retry cron). No data loss. Fix: tolerate rename failure when the dedup marker is committed / check for the .processed sibling.

6. [Poisoned file vs circuit breaker semantics] models/edi_processor.py:166-178 + models/edi_trading_partner.py:515-534 — a persistently-failing file trips the breaker (suspending ALL polling for the partner one 60-min cooldown), after which circuit_open_since is never refreshed on half-open failure so the breaker degrades to a permanent no-op while the file re-fails every 15 min (alert rate-limited to 1/hr). Bounded impact but incoherent semantics; decide explicitly — refresh circuit_open_since on failed half-open, and/or quarantine the file (rename .failed) after N attempts instead of duty-cycling the whole partner.

7. [Un-cancel raises raw UniqueViolation] models/sale_order.py:27-33 — pressing 'Set to Quotation' on a cancelled EDI SO after a replacement live SO exists re-enters the partial-index predicate and surfaces an opaque psycopg2 error. Blocking the un-cancel is the right business outcome; add an @api.constrains or action_draft override with a readable UserError.

8. [Issue-workflow asymmetry] models/edi_order_review.py:246-273 — action_approve_corrected never accepts the qty_shortfall issues _reclamp_before_confirm creates for zero-clamps, leaving them 'pending' forever on an approved review; and apply_change_order (edi_processor.py:1026-1047) creates no qty_shortfall issue at all for ORDCHG zero-clamps (chatter+log only). Align both with _approve_new_order's accept step.

## Verified OK

- [R3's in_test_mode blocker is ALREADY FIXED in the working tree] models/edi_processor.py:74-92 defines _commit_suppressed (getattr on registry.in_test_mode with fallback to odoo.tools.config['test_enable'], fail-toward-suppress), and BOTH commit sites use it (edi_processor.py:418, edi_order_review.py:530 via import). Grep confirms no remaining direct self.env.registry.in_test_mode() call anywhere in the module — the reviewer adjudicated an earlier snapshot of the diff. No action needed; do not re-litigate.
- IDEM-2 ordering invariant (process → dedup marker → COMMIT → FTP rename → ACK/OOS) verified correctly sequenced at edi_processor.py:352-376 with reachable self-heal (hash dup-skip + re-rename at :308-315; missed ACK covered by retry cron).
- IDEM-1a advisory lock verified: pg_advisory_xact_lock(0x45444950 fits int4, partner.id), re-acquired after every commit including the ACK claim commit inside a poll, correctly detected via the edi_reservation_warned context set placed by poll_trading_partner (:279, edi_order_review.py:537).
- IDEM-1c savepoint conversion verified: the unique violation fires inside the per-store savepoint (edi_processor.py:510-511), _is_duplicate_so_violation matches on the index name, and the duplicate_skipped log row is written after rollback so it survives (:513-532).
- Multi-PO ACK guard in briscoes_idoc.generate_ack fails closed and leaves single-PO files byte-identical; _send_file_responses now covers every PO in a file, not just the first (edi_processor.py:437-461).
- IDEM-4 attempt bookkeeping coherent: _ack_exchange_filename is the single source of truth across _queue_ack/_compute_ack_status/_supersede_sent_ack/retry_pending_acks; attempt 1 preserves historical filenames so existing prod ack_sent rows still match; supersede bumps all sibling store-reviews together (edi_order_review.py:364-398).
- Partial unique index predicate (state != 'cancel') correctly permits the deliberate re-order-after-cancellation path (edi_processor.py:570-587), and Odoo 19 sale's client_order_ref copy=False keeps UI Duplicate outside the predicate.
- OPS-25 accounting sane: per-file failures count once per poll toward the breaker, clean polls reset (edi_processor.py:151-164), file-failure alerts use their own rate-limit bucket (:423-435), and the partial-failure _poll_commit (:340) durably persists succeeded stores' dedup rows so retries only re-attempt failed stores.
- Reclamp math itself and its fail-closed availability (lookup errors → 0.0), the backorder no-op gate, and the non-EA UoM accept-in-full guard (edi_processor.py:770-779, 1070-1090) are correct as designed — the two must-fix items are missing GUARDS around when the reclamp runs, not the math.
